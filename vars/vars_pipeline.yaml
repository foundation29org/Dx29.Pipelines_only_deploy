variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: <docker_registry_connection>
  containerRegistry: <acr_server>
  imagePullSecret: <aks_image_pull_secret_name>

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

  environment: <Environment.namespace>

  # Images
  imageRepositorySample: 'sample'
